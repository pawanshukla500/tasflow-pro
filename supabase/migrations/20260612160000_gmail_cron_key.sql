-- Placeholder migration: gmail_cron_key vault secret is provisioned manually
-- (Dashboard → Vault or scripts/setup-report-cron.sql pattern).
-- gmail_sync_cron (20260612150000) schedules the job once the secret exists.

SELECT 1;
