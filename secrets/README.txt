# Copy your Firebase service account JSON here (from Firebase Console).
# File name must match: FIREBASE_SERVICE_ACCOUNT_JSON_PATH in .env
#
# Firebase Console → Project settings → Service accounts → Generate new private key
# Save the downloaded file as:  firebase-service-account.json  (in this folder)
#
# This file is gitignored — never commit real keys.
